.show-wrpper
  = render "homes/header"
  .show-header
    .show-userimage
      = image_tag @user.image.url, class: "image"
    .show-username
      = current_user.name
      .edit-btn
        = link_to "編集", edit_user_path(current_user.id), class: "edit"